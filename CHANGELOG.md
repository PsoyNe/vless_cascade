# Changelog

Все значимые изменения в проекте документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

---

## [1.0.7] - 2026-09-22

### Added

- **Двойная проверка резервных ссылок.**
  - Новая функция `test_link_double_check` в `vless_common.py`.
  - Перед добавлением в резерв ссылка проверяется **N раз с интервалом**.
  - В резерв попадают только те, которые **ВСЕ N проверок** прошли успешно.
  - Отсеивает ссылки, которые отвечают **1 раз из N** — нестабильные.
  - Работает **и при старте**, **и во время работы** (в `_fill_backup_pool_*`).

- **Проверка перед переключением.**
  - В `switch_to_backup` — перед тем как поставить ссылку как основную, observer **проверяет её прямо сейчас**.
  - Если ссылка **мертва** — в deferred, берётся **следующий** кандидат из пула.
  - **Больше не переключаемся на мёртвую.**

- **Новые параметры в `observer_config.py`:**
  ```python
  # Сколько раз проверять ссылку перед добавлением в резерв.
  BACKUP_CHECK_ATTEMPTS = 2

  # Пауза между проверками (сек).
  BACKUP_CHECK_INTERVAL = 2
  ```

- **Устойчивое чтение параметров** в `vless_common.py`.
  - Если параметров нет в конфиге (пользователь отказался добавлять при merge) — берутся дефолты через `getattr`.

### Changed

- **Логи стали информативнее.**
  - При пополнении резерва:
    ```
    🔎 Проверяем кандидата [1]: 31.76.39.141 [GB] (2 проверок, интервал 2с)...
    ❌ 31.76.39.141 [GB] не прошла 2 проверок — в deferred
    🔎 Проверяем кандидата [2]: 147.45.148.6 [RU] (2 проверок, интервал 2с)...
    ✅ 147.45.148.6 [RU] живая — в резерв [4/4] (пинг: 583мс)
    ```
  - При переключении:
    ```
    🔎 Проверка перед переключением: 31.76.57.191 [DE]...
    ✅ 31.76.57.191 [DE] жива (пинг: 747мс) — переключаемся
    ```

- **Логи при старте observer'а** — добавлены строки:
  ```
  Двойная проверка резерва: 2 раз, интервал 2с
  Проверка перед переключением: включена
  ```

### Fixed

- **Проблема «резервные вроде есть, но при переключении связь сразу падает».**
  - Раньше: одна успешная проверка давала ложное «жива». Ссылка, которая отвечает 1 раз из 2, попадала в резерв и **сразу отваливалась** при переключении.
  - Теперь: ссылка проходит **двойную проверку** при добавлении в резерв и **проверку перед переключением**. Плохие отсеиваются заранее.

---

## [1.0.6] - 2026-09-22

### Added

- **Приоритетная страна (`PREFERRED_COUNTRY`).**
  - Если задан код страны (например, `'DE'`) — observer в первую очередь берёт ссылки с этой страной при поиске основной и пополнении резерва.
  - Если пустая строка — приоритета нет.
  - Если код невалидный — игнорируется с предупреждением.
  - Приоритет — **не ограничение**: если ссылок с этой страной нет, observer добирает из остальных.

- **Система триггеров через файлы в `/tmp/`** (`triggers.py`).
  - Управление observer'ом из Telegram-бота или вручную.
  - **Проверка свежести:** триггер старше `TRIGGER_MAX_AGE` (30 сек) удаляется без выполнения.
  - **Чистка при старте:** все старые триггеры удаляются при запуске observer'а.
  - **Ответы** — в `/tmp/vless_<action>_response` в формате **JSON**.

- **Триггеры:**

  | Файл | Действие |
  |------|----------|
  | `/tmp/vless_status_trigger` | Запрос статуса |
  | `/tmp/vless_links_trigger` | Список всех ссылок |
  | `/tmp/vless_switch_trigger` | Переключиться на следующую |
  | `/tmp/vless_geo_DE` | Переключиться на страну DE |
  | `/tmp/vless_pick_46.28.69.53` | Переключиться на host |
  | `/tmp/vless_ignore_46.28.69.53` | В карантин |
  | `/tmp/vless_unignore_46.28.69.53` | Из карантина |
  | `/tmp/vless_reload_trigger` | Перечитать файл ссылок |

- **Новый модуль `vless_common.py`.**
  - Общие функции для `observer.py` и `triggers.py`.
  - Устранение циклических импортов.

- **Новый модуль `triggers.py`.**
  - Логика всех триггеров.

- **Новые параметры в `observer_config.py`:**
  ```python
  PREFERRED_COUNTRY = ""
  TRIGGER_MAX_AGE = 30
  ```

### Changed

- **`observer.py` разделён на модули.**
  - `vless_common.py` — общие функции.
  - `triggers.py` — триггеры.
  - `observer.py` — логика observer'а.

- **`test_primary` сначала проверяет триггеры.**

- **`install.sh` и `update.sh` обновлены** — скачивают новые модули.

---

## [1.0.5] - 2026-09-17

### Added

- **Показ `fail_count` в логе статуса observer'а.**
  - Показывается **только если** `fail_count > 0`.

- **Защита от «круговорота» основной ссылки (`recently_primary`).**
  - После переключения старая основная **исключается из кандидатов** на `RECENTLY_PRIMARY_COOLDOWN` секунд.

- **Новый параметр:**
  ```python
  RECENTLY_PRIMARY_COOLDOWN = 60
  ```

---

## [1.0.4] - 2026-09-17

### Fixed

- **Observer проверяет резервные ссылки перед добавлением в пул.**
  - Раньше: брал следующую из файла **без проверки**.
  - Теперь: **проверяет через Xray** в момент пополнения пула.

- **`switch_to_backup` использует проверку кандидатов.**

- **`update_pool_from_file` больше не добавляет ссылки в пул без проверки.**

### Changed

- **`refill_pool_excluding_deferred` заменён на `_fill_backup_pool_checked`.**

---

## [1.0.3] - 2026-09-14

### Added

- **Определение гео ссылок** (`vless_checker.py`).
  - Чекер запрашивает страну через Cloudflare trace.
  - Результат — в fragment `#COUNTRY`.
  - Если не определилось — `#UNKNOWN`.

- **Устойчивый импорт гео-параметров** через `getattr`.

- **Показ гео в логах observer'а.**
  - `[DE]`, `[NL]`, `[US]`, `[UNKNOWN]`, `[N/A]`.

- **Гео-распределение в логе чекера.**

- **Новые параметры:**
  ```python
  GEO_ENABLED = True
  GEO_URL = "https://www.cloudflare.com/cdn-cgi/trace"
  GEO_TIMEOUT = 5
  GEO_UNKNOWN_MARK = "UNKNOWN"
  ```

### Changed

- **`vless_checker.py`: fragment отсекается перед парсингом.**
- **`observer.py`: сравнение ссылок — без гео.**

---

## [1.0.2] - 2026-09-14

### Added

- **Умное слияние конфигов при обновлении** (`merge_config.py`).
  - Дополняет пользовательские конфиги новыми параметрами.
  - Существующие параметры **не трогаются**.
  - Спрашивает подтверждение (`y/n`).

- **`update.sh` обновляет конфиги через `merge_config.py`.**

- **`install.sh` скачивает `merge_config.py`.**

### Changed

- **`update.sh` разделяет обновление кода и конфигов.**

---

## [1.0.1] - 2026-09-14

### Fixed

- **Observer проверяет ссылки при старте.**
  - Перебирает ссылки по одной и ставит в outbound **только живую**.
  - Резерв наполняется **только живыми**.

- **`init_pool` возвращает `bool`.**
- **`run()` — цикл с ожиданием.**

### Changed

- **Старт observer занимает больше времени.**

---

## [1.0.0] - 2026-09-14

Первый публичный релиз. **Alpha 1.0.**

### Added

- **Этап 1: сбор VLESS-ссылок** (`vless_checker.py`).
- **Observer: трёхпоточный наблюдатель** (`observer.py`).
- **Ротация логов.**
- **Graceful shutdown.**
- **Триггеры для отладки.**
- **`show_logs.sh`** — меню логов.
- **`install.sh`** — установщик.
- **Защита от двойного запуска чекера** через `flock`.
- **Автообновление через `update.sh`.**

### Changed

- **Observer читает `working_links.txt`** (30 ссылок).
- **Отложенные ссылки перепроверяются раз в 10 минут.**
- **Валидация Reality-параметров.**
- **Обновление `working_links.txt` через атомарную запись.**
- **`reload_xray` больше не использует SIGHUP.**
- **`switch_to_backup` — цикл вместо рекурсии.**

### Fixed

- **`start_time` в начале `test_link_through_xray`.**
- **Сокеты Xray корректно закрываются в `finally`.**
- **`self.primary is None` в финальном логе.**
- **Защита от гонок** через `threading.Lock`.
- **Реальная остановка `until_30`** в чекере.
- **`STAGE2_MIN_RESPONSE_SIZE` теперь используется.**
- **`systemd` юнит `vless_observer`** корректно завершает службу.

### Removed

- **`full_check.py`** — не использовался.

---

## [Unreleased]

Изменения, которые войдут в следующий релиз.

### Added

- **Telegram-бот** (в разработке — отдельный проект).

### Changed

-

### Fixed

-

### Removed

-
